{
 "_declared": "RECALIBRATED 2026-08-05 on a dedicated null of 600 control runs, seed block 950000+ (fresh, disjoint from campaign 1's calibration block 900000-900039 and from every scored seed, all of which are < 900000). Configuration c=1.0, white/mid, MNAR -- identical to campaign 1's calibration configuration; only the sample size and the FPR target changed. Rule FORM is unchanged from configs/detection_rule.json: only the leading threshold k and the lagging threshold k_sd were re-picked. Selection, declared in advance: leading = smallest k with calibration null FPR <= 3.5%; lagging = among rules with calibration null FPR <= 3.5% and calibration detection rate >= 0.5, the earliest-detecting. Applied UNCHANGED at every coverage level, noise colour, severity and consent arm. See spec section 9 amendment A-8.",
 "supersedes": "configs/detection_rule.json",
 "nominal_fpr_target": 0.035,
 "external_fpr_bar": 0.036,
 "n_calibration_seeds": 600,
 "calibration_seed_base": 950000,
 "scored_seed_ceiling": 900000,
 "leading": {
  "statistics": [
   "rolling_variance",
   "rolling_lag1_autocorrelation"
  ],
  "detrend": "in-window linear",
  "window": 30,
  "classes": [
   "obs_formation"
  ],
  "class_mixing": "mean of per-class standardised statistic (variance-family only; no covariance across node pairs)",
  "standardisation": "z against the same run's baseline period [window-1, 90)",
  "combine": "mean",
  "k": 6.5,
  "sustain": 5,
  "start": 90,
  "min_frac": 0.5,
  "max_gap": 3
 },
 "lagging": {
  "metric": "program-close output, trailing mean over 10 cycles",
  "baseline_end": 90,
  "k_sd": 3.5,
  "sustain": 3,
  "max_gap": 3
 },
 "calibration_result": {
  "leading": {
   "k": 6.5,
   "calibration_fpr": 0.035,
   "n_false_alarms": 21,
   "n_null": 600,
   "previous_k": 5.0,
   "previous_k_fpr_on_this_null": 0.11
  },
  "lagging": {
   "k_sd": 3.5,
   "sustain": 3,
   "fpr": 0.02666666666666667,
   "n_false_alarms": 16,
   "detect_rate": 0.75,
   "median_t": 262.0
  }
 }
}